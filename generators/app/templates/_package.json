{
  "name": "<%= appname %>",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm test && npm run build",
    "test": "npm run lint && mocha",
    "build": "babel -d dist src",
    "lint": "eslint ."
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "chai": "^3.2.0",
    "eslint": "^1.2.0",
    "mocha": "^2.2.5",
    "sinon": "^1.16.0",
    "sinon-chai": "^2.8.0"
  }
}
