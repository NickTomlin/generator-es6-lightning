{
  "name": "<%= _.kebabCase(appname) %>",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm test && npm run build",
    "test": "",
    "build": "babel -d dist src",
    "lint": "eslint ."
  },
  "devDependencies": {
    "babel": "^5.8.21",
    "eslint": "^1.2.0"
  }
}
